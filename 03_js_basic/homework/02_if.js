/**
 * 問1. 変数test1の中身を自由に変更して、ifかelseに処理がいくのか検証してください
 * 正当はありません
 */
const test1 = true;
if (test1) {
    console.log('true');
} else {
    console.log('false');
}

/**
 * 問2. 論理演算子を調査し、記載してください
 */

/**
 * 問2. 回答欄
 * AND は &&
 * OR は ||
 * NOT は ! 
 */

/**
 * 問3. 論理演算子を使って、test3のconsoleでbase paramを表示させてください。
 * 変数baseの値を変更して回答してください
 */
const base1 = false;
const test3 = base1 || 'base param';
console.log(test3);

/**
 * 問4. 三項演算子を調査し、記載してください
 */

/**
 * 問4. 回答欄
 * if文のショートカットとして用いられる。
 * 条件 ? 真のときに実行する式 : falsyであった場合に実行する式
 * の形で用いられる
 */

/**
 * 問5. 三項演算子を使って、test5のconsoleでright paramを表示させてください。
 * 変数baseの値を変更して回答してください
 */
const base2 = false;
const test5 = base2 ? 'left param' : 'right param';
console.log(test5);



/**
 * 問6. if文とは何か、説明してください
 */

/**
 * 問6. 回答欄
 *「 もし、〇〇だったらこうする。そうでなかったらこうする。」
 * といった条件分岐に用いるもの
 * 
 */

